# Enough to run the tests without the heavy parts (torch, CUDA, Whisper, Kokoro, ChromaDB).
# CI uses this. tests/test_memory.py needs the full requirements.txt (it downloads a model).
pytest>=8.0.0
pytest-mock>=3.14.0
pyyaml>=6.0.2
numpy>=1.26.0
openai>=1.0.0
ollama>=0.4.0
fastapi>=0.115.0
uvicorn>=0.34.0
httpx>=0.27.0
ddgs>=9.0.0
pyautogui>=0.9.54
sounddevice>=0.5.0
